version: '3.8'

services:
#  db:
#    image: postgres:18
#    container_name: hostel-db
#    environment:
#      POSTGRES_DB: hostel_management_system
#      POSTGRES_USER: postgres
#      POSTGRES_PASSWORD: "$$@nju#29" # Use your actual variable or secret
#    volumes:
#      - ./hostel_management_dump.sql:/docker-entrypoint-initdb.d/init.sql
#    ports:
#      - "5432:5432"
#    healthcheck:
#      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
#      interval: 5s
#      timeout: 5s
#      retries: 5
  redis:
    image: redis:7
    container_name: hostel-redis
    ports:
      - "6379:6379"
  kafka:
    image: apache/kafka:latest
    container_name: hostel-kafka
    ports:
      - "29092:29092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://:29092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:29092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
#  app:
#    build: .
#    container_name: hostel-app
#    depends_on:
#      db:
#        condition: service_healthy
#    ports:
#      - "8083:8083"
#    environment:
#      SPRING_PROFILES_ACTIVE: docker
