spring:
  datasource:
    url: jdbc:postgresql://db:5432/hostel_management_system
    username: postgres
    password: $@nju#29
    hikari:
      initialization-fail-timeout: 60000
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect