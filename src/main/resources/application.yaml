jwt:
  secret: RrURV5twHjsX+LPK3MfI67swocN972ymHopqr3rAANw=
  expiration: 86400000
server:
  port: 8083
  servlet:
    context-path: /api
logging:
  level:
    org.springframework.security: DEBUG
spring:
  application:
    name: hostel-managment-system
  datasource:
    url: jdbc:postgresql://localhost:5432/hostel_management_system
    username: postgres
    password: $@nju#29
    jpa:
      properties:
        hibernate:
          default_schema: hostel
      hibernate:
        ddl-auto: none
      show-sql: true